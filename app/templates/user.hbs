{{page-title 'User'}}

<Layout @hideFooter={{true}}>
  <UserProfile
    @id={{@model.id}}
    @created={{@model.created}}
    @karma={{@model.karma}}
    @about={{@model.about}}
  />
</Layout>
{{outlet}}