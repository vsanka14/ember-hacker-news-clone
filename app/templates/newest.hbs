{{page-title 'New Links | Hacker News' replace=true}}

<Layout>
  <Stories
    @stories={{@model}}
    @page={{this.page}}
    @toggleItemVisibility={{this.toggleItemVisibility}}
  />
</Layout>
{{outlet}}