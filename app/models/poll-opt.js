export default class PollOpt {
  constructor({ by, id, poll, score, text, time, type }) {
    this.by = by;
    this.id = id;
    this.poll = poll;
    this.score = score;
    this.text = text;
    this.time = time;
    this.type = type;
  }
}
